{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --include=dev && npx prisma generate --schema=packages/database/prisma/schema.prisma && cd apps/api && npx nest build"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=packages/database/prisma/schema.prisma && node apps/api/dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
